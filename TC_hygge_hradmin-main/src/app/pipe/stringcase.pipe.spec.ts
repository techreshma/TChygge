import { StringcasePipe } from './stringcase.pipe';

describe('StringcasePipe', () => {
  it('create an instance', () => {
    const pipe = new StringcasePipe();
    expect(pipe).toBeTruthy();
  });
});
